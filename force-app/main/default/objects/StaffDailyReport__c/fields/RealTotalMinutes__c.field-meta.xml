<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RealTotalMinutes__c</fullName>
    <externalId>false</externalId>
    <formula>(VALUE(LEFT( EndWork__c , FIND(&quot;:&quot;, EndWork__c)-1)) * 60 + VALUE(RIGHT( EndWork__c , 2))) - (VALUE(LEFT( StartWork__c , FIND(&quot;:&quot;, StartWork__c)-1)) * 60 + VALUE(RIGHT( StartWork__c , 2))
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>（計算用）実労働時間（分）</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
